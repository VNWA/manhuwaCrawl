{
  "name": "crawl",
  "module": "index.ts",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "javascript-obfuscator": "^4.1.1",
    "tsc-alias": "^1.8.16"
  },
  "scripts": {
    "build": "tsc && tsc-alias && javascript-obfuscator dist --output dist --compact true --control-flow-flattening true",
    "start": "node dist/index.js",
    "build:zip": "rm -f deploy.zip && npm run build && zip -r deploy.zip dist package.json bun.lock .env README.md"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "cheerio": "^1.1.2",
    "p-limit": "^7.1.1",
    "prisma": "^6.16.2",
    "puppeteer": "^24.22.3"
  }
}